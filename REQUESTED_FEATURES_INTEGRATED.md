این نسخه بر پایه پروژه اصلی ساخته شده و فقط قابلیت‌های درخواستی را به رابط فعلی متصل می‌کند:

1) پیام رمزدار در گروه:
- دکمه قفل فقط در گروه کنار کادر پیام ظاهر می‌شود.
- پیام با AES-256-GCM و PBKDF2 رمزگذاری می‌شود.
- رمز در دیتابیس ذخیره نمی‌شود.
- گیرنده برای مشاهده پیام رمز را وارد می‌کند.

2) رمز ورود حساب:
- ورود فعلی پروژه همچنان با رمز عبور انجام می‌شود.
- بخش «تغییر رمز عبور» موجود در تنظیمات حفظ شده است.

3) تأیید دو مرحله‌ای:
- از تنظیمات فعال می‌شود.
- TOTP/Authenicator با Supabase MFA استفاده می‌شود.
- پس از ورود، اگر 2FA فعال باشد، ورود تا وارد کردن کد ۶ رقمی متوقف می‌شود.

پس‌زمینه و فایل‌های theme اولیه پروژه تغییر داده نشده‌اند.
